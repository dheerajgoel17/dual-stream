/**
 * Optional support links for the extension popup.
 * Keep in sync with .github/FUNDING.yml — that file drives the repo Sponsor button.
 * Docs: docs/SUPPORT.md
 */
(function (root) {
  const api = {
    repo: "https://github.com/dheerajgoel17/dual-stream",
    githubSponsors: "https://github.com/sponsors/dheerajgoel17",
    buyMeACoffee: "https://buymeacoffee.com/meghnaad",
  };

  root.DualStreamSupport = api;
})(typeof globalThis !== "undefined" ? globalThis : window);
